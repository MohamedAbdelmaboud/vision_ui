import 'package:flutter/material.dart';

class TransactionsInformation extends StatelessWidget {
  const TransactionsInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
